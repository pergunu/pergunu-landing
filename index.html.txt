<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PERGUNU Situbondo</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to bottom, #f0f4c3, #e8f5e9);
      color: #333;
      text-align: center;
      overflow: hidden;
    }
    .container {
      padding: 40px 20px;
    }
    .logo img {
      width: 140px;
      max-width: 90%;
      animation: fadeInDown 2s ease-out;
    }
    .title {
      margin-top: 20px;
      font-size: 24px;
      color: gold;
      font-weight: bold;
      animation: fadeIn 3s ease-out;
    }
    .subtitle {
      font-size: 28px;
      color: green;
      font-weight: 700;
      margin-top: 10px;
      animation: fadeInUp 3s ease-out;
    }
    .footer {
      position: fixed;
      bottom: 10px;
      width: 100%;
      font-size: 14px;
      color: #555;
    }

    /* Animasi sederhana */
    .sparkle {
      position: absolute;
      width: 6px;
      height: 6px;
      background: gold;
      border-radius: 50%;
      opacity: 0.7;
      animation: sparkle 5s linear infinite;
    }
    @keyframes sparkle {
      0% {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
      100% {
        transform: translateY(100vh) scale(0.5);
        opacity: 0;
      }
    }
    @keyframes fadeInDown {
      from {opacity: 0; transform: translateY(-30px);}
      to {opacity: 1; transform: translateY(0);}
    }
    @keyframes fadeInUp {
      from {opacity: 0; transform: translateY(30px);}
      to {opacity: 1; transform: translateY(0);}
    }
    @keyframes fadeIn {
      from {opacity: 0;}
      to {opacity: 1;}
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <img src="https://drive.google.com/uc?export=view&id=1XaYh2eDaqvMB