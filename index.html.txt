<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Halaman Pembuka PERGUNU</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #e8f5e9, #c8e6c9);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
    }

    .container {
      background: white;
      padding: 30px;
      border-radius: 16px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      animation: fadeIn 1.5s ease-in-out;
      max-width: 600px;
    }

    @keyframes fadeIn {
      from {opacity: 0;}
      to {opacity: 1;}
    }

    .logo {
      width: 120px;
      margin-bottom: 20px;
    }

    .buttons {
      margin-top: 25px;
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .btn-yes {
      background-color: #4CAF50;
      color: white;
    }

    .btn-no {
      background-color: #f44336;
      color: white;
    }

    footer {
      margin-top: 40px;
      font-size: 12px;
      color: #555;
    }
  </style>
</head>
<body>

  <div class="container" id="landing">
    <img class="logo" src="https://drive.google.com/uc?export=view&id=1XaYh2eDaqvMBhWYFZt-kwMfBi4S9gGDJ" alt="Logo PERGUNU">
    <h2>Selamat Datang di Website Resmi PERGUNU Situbondo</h2>
    <p>Apakah Anda ingin mengakses halaman utama website?</p>
    <div class="buttons">
      <button class="btn-yes" onclick="masuk()">Iya, Lanjutkan</button>
      <button class="btn-no" onclick="keluar()">Keluar</button>
    </div>
  </div>

  <div id="konten" style="display:none; padding: 20px; text-align: left;">
    <div style="display: flex; flex-wrap: wrap; justify-content: space-between; align-items: center;">
      <div>
        <h3 id="tanggal"></h3>
        <p id="jam" style="font-size: 20px; font-weight: bold;"></p>
      </div>
      <div style="border-left: 2px solid #ccc; padding-left: 20px; max-width: 60%;">
        <h3 style="color: #2e7d32;">Jangan Lupa Melaksanakan Sholat & Membaca Al-Qur'an</h3>
      </div>
    </div>
    <audio id="audio" autoplay loop hidden>
      <source src="https://docs.google.com/uc?export=download&id=1Vl7s18sZpFN9KcY7lZNWGZORt4ADP1Co" type="audio/mpeg">
    </audio>
  </div>

  <footer>
    Website ini dikembangkan oleh cendhanu@pergunusitubondo
  </footer>

  <script>
    function masuk() {
      document.getElementById("landing").style.display = "none";
      document.getElementById("konten").style.display = "block";
      document.getElementById("audio").play();
    }

    function keluar() {
      alert("Terima kasih telah mengakses website PERGUNU.");
    }

    function updateJam() {
      const now = new Date();
      const jam = now.getHours().toString().padStart(2, '0');
      const menit = now.getMinutes().toString().padStart(2, '0');
      const detik = now.getSeconds().toString().padStart(2, '0');
      document.getElementById("jam").textContent = `${jam}:${menit}:${detik}`;

      const hari = ["Minggu", "Senin", "Selasa", "Rabu", "Kamis", "Jumat", "Sabtu"];
      const bulan = [
        "Januari", "Februari", "Maret", "April", "Mei", "Juni",
        "Juli", "Agustus", "September", "Oktober", "November", "Desember"
      ];
      const tanggal = `${hari[now.getDay()]}, ${now.getDate()} ${bulan[now.getMonth()]} ${now.getFullYear()}`;
      document.getElementById("tanggal").textContent = tanggal;

      const alarm = {
        subuh: "04:30",
        dzuhur: "12:00",
        ashar: "15:30",
        maghrib: "18:00",
        isya: "19:15"
      };
      const sekarang = `${jam}:${menit}`;
      for (let [nama, waktu] of Object.entries(alarm)) {
        if (sekarang === waktu) {
          alert(`Sudah masuk waktu sholat ${nama.toUpperCase()}`);
        }
      }
    }

    setInterval(updateJam, 1000);
    updateJam();
  </script>
</body>
</html>