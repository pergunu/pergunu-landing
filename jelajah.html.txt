<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>PERGUNU Situbondo</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #1e3c72, #2a5298);
      color: white;
      overflow: hidden;
    }
    .frame {
      background: rgba(0, 0, 0, 0.4);
      border-radius: 20px;
      padding: 30px;
      margin: 80px auto;
      max-width: 650px;
      text-align: center;
      box-shadow: 0 0 25px rgba(255, 255, 255, 0.2);
    }
    h1 {
      font-size: 2em;
      margin: 0;
      color: #ffd700;
    }
    h2 {
      font-size: 3em;
      margin-top: 10px;
      font-weight: bold;
      color: #00ff7f;
    }
    .button-container {
      margin-top: 30px;
      display: flex;
      justify-content: center;
      gap: 30px;
    }
    button {
      padding: 12px 30px;
      font-size: 16px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: 0.3s;
      width: 150px;
    }
    #yesBtn {
      background-color: #28a745;
      color: white;
    }
    #noBtn {
      background-color: #dc3545;
      color: white;
    }
    #yesBtn:hover {
      background-color: #218838;
    }
    #noBtn:hover {
      background-color: #c82333;
    }
    footer {
      background-color: #f4c430;
      color: black;
      text-align: center;
      padding: 15px;
      position: absolute;
      bottom: 0;
      width: 100%;
      font-weight: bold;
    }
    canvas {
      position: fixed;
      top: 0;
      left: 0;
      z-index: -1;
    }
  </style>
</head>
<body>

<canvas id="particles"></canvas>

<div class="frame">
  <img src="https://raw.githubusercontent.com/pergunu/pergunu-landing/refs/heads/main/logo-pergunu.png" alt="Logo PERGUNU" width="120" />
  <h1>Selamat Datang Di Website Resmi</h1>
  <h2>PERGUNU SITUBONDO</h2>

  <p style="margin-top: 20px; font-size: 18px;">Apakah Anda ingin mengunjungi halaman website PERGUNU?</p>
  <div class="button-container">
    <button id="noBtn">Keluar</button>
    <button id="yesBtn">Iya, Masuk</button>
  </div>
</div>

<footer>Website ini dikembangkan oleh cendhanu tim kreatif PERGUNU Situbondo sejak 2025</footer>

<audio id="audio" autoplay>
  <source src="https://media.vocaroo.com/mp3/1ooLLSC7CVfx" type="audio/mpeg">
</audio>

<script>
  const audio = document.getElementById("audio");

  document.getElementById("yesBtn").addEventListener("click", () => {
    audio.play().then(() => {
      setTimeout(() => {
        window.location.href = "https://sites.google.com/view/pergunusitubondo/menu";
      }, 500); // jeda 0.5 detik agar audio sempat mulai
    }).catch(() => {
      window.location.href = "https://sites.google.com/view/pergunusitubondo/menu";
    });
  });

  document.getElementById("noBtn").addEventListener("click", () => {
    alert("Terima kasih atas kunjungan Anda di website kami.");
    window.close();
  });

  // Animasi partikel latar
  const canvas = document.getElementById("particles");
  const ctx = canvas.getContext("2d");
  let particles = [];

  function resizeCanvas() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  }

  function createParticles() {
    particles = [];
    for (let i = 0; i < 80; i++) {
      particles.push({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        r: Math.random() * 2 + 1,
        d: Math.random() * 1
      });
    }
  }

  function drawParticles() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = "rgba(255,255,255,0.8)";
    ctx.beginPath();
    particles.forEach(p => {
      ctx.moveTo(p.x, p.y);
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2, true);
    });
    ctx.fill();
    moveParticles();
  }

  function moveParticles() {
    particles.forEach(p => {
      p.y += p.d;
      if (p.y > canvas.height) {
        p.y = 0;
        p.x = Math.random() * canvas.width;
      }
    });
  }

  window.addEventListener("resize", () => {
    resizeCanvas();
    createParticles();
  });

  resizeCanvas();
  createParticles();
  setInterval(drawParticles, 33);
</script>

</body>
</html>